--- 1.100.0/litellm/router_utils/prompt_caching_cache.py
+++ 1.101.0rc1/litellm/router_utils/prompt_caching_cache.py
@@ -18,7 +18,7 @@
     from litellm.router import Router
 
     litellm_router = Router
-    Span = _Span | Any
+    Span = _Span
 else:
     Span = Any
     litellm_router = Any
@@ -34,7 +34,7 @@
         self.in_memory_cache = InMemoryCache()
 
     @staticmethod
-    def serialize_object(obj: Any) -> Any:
+    def serialize_object(obj: Any) -> object:
         """Helper function to serialize Pydantic objects, dictionaries, or fallback to string."""
         if hasattr(obj, "dict"):
             # If the object is a Pydantic model, use its `dict()` method
