--- 1.100.0/litellm/proxy/management_endpoints/customer_endpoints.py
+++ 1.101.0rc1/litellm/proxy/management_endpoints/customer_endpoints.py
@@ -626,11 +626,7 @@
         # get non default values for key
         non_default_values: Final = dict[str, object]()
         for k, v in data_json.items():
-            if v is not None and v not in (
-                [],
-                {},
-                0,
-            ):  # models default to [], spend defaults to 0, we should not reset these values
+            if v is not None and ((isinstance(v, bool) and k in data.fields_set()) or v not in ([], {}, 0)):
                 non_default_values[k] = v
 
         ## Get end user table data ##
